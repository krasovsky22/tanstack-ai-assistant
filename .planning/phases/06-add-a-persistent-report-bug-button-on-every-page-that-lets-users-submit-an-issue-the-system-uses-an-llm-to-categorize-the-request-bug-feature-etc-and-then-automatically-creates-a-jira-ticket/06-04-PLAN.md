---
phase: 06-report-bug-button
plan: 04
type: execute
wave: 3
depends_on:
  - "06-03"
files_modified: []
autonomous: false
requirements:
  - RPT-05
  - RPT-06

must_haves:
  truths:
    - "Header bar is visible on all protected pages"
    - "Header is absent on the login page"
    - "Page content is not obscured by the fixed header"
    - "Clicking Report Issue opens the modal"
    - "Submitting the form with a real title and description creates a Jira ticket"
    - "Success state shows the category label and a link to the created ticket"
  artifacts:
    - path: "src/components/AppHeader.tsx"
      provides: "Fixed header with report entry point"
    - path: "src/components/ReportIssueModal.tsx"
      provides: "Report form with LLM classification flow"
  key_links:
    - from: "AppHeader report button"
      to: "ReportIssueModal"
      via: "onOpen state in AppLayout"
      pattern: "isReportOpen"
---

<objective>
Human verification of the complete Report Issue feature. Playwright QA agent validates the header is visible on protected pages, absent on login, and the full submit → classify → Jira ticket creation flow works end-to-end.

Purpose: Confirm the feature works in a real browser before phase is marked complete.
Output: Playwright tests passing; phase marked done.
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-a-persistent-report-bug-button-on-every-page-that-lets-users-submit-an-issue-the-system-uses-an-llm-to-categorize-the-request-bug-feature-etc-and-then-automatically-creates-a-jira-ticket/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run unit test suite gate</name>
  <files></files>
  <action>
    Run the full test suite to confirm all previous plans' implementations are green before handing off to Playwright QA.

    Commands:
    1. `pnpm vitest run src/lib/report-issue.test.ts` — confirm RPT-01 through RPT-04 pass
    2. `pnpm test` — confirm no regressions across the full suite

    If any tests fail, surface the error output and stop. Do not proceed to the Playwright checkpoint until the full suite is green.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    pnpm test exits 0 — all tests pass including report-issue.test.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright QA — invoke playwright-qa-tester agent</name>
  <files></files>
  <action>
    Per CLAUDE.md: "For any task that involves creating, modifying, or refactoring UI components, pages, or visual features, always delegate to the `playwright-qa-tester` agent via the Agent tool."

    Invoke the `playwright-qa-tester` agent to write and execute Playwright tests against the running dev server (port 3000).

    Scenarios to validate:
    1. **Header absent on login**: Navigate to `/login` — assert AppHeader is NOT present (no "Report Issue" button visible).
    2. **Header present on protected pages**: Log in, navigate to `/` (or any protected route) — assert a button with text "Report Issue" is visible in the top area.
    3. **Header does not overlap content**: On a protected page, assert the first heading or main content element is not obscured (its top offset is greater than 56px from the viewport top).
    4. **Modal opens**: Click the "Report Issue" button — assert a modal dialog appears with a Title input and Description textarea.
    5. **Form submission flow**: Fill in Title ("Test bug report") and Description ("This is a test submission for QA"), click Submit — assert the modal enters a loading state (spinner visible or submit button disabled), then transitions to a success state showing a category label ("Bug report created" or "Feature request created" or "Task created") and a Jira ticket link.
    6. **Error preservation**: If the Jira integration is not configured, assert the modal shows an inline error message and the form fields still contain the submitted values (not reset).

    The agent runs against `http://localhost:3000`. Dev server must be running (`pnpm dev`).
  </action>
  <verify>
    <automated>MISSING — Playwright QA agent writes and runs tests during execution</automated>
  </verify>
  <done>
    Playwright QA agent reports all 6 scenarios passing (or provides a written verification summary confirming observed behavior matches expected).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — full Report Issue flow</name>
  <files></files>
  <action>
    Human visually verifies the complete Report Issue feature end-to-end in the browser. See how-to-verify steps below.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>User types "approved" after confirming all 7 verification steps pass.</done>
  <what-built>
    Complete Report Issue feature:
    - Fixed AppHeader on all protected pages (not login) with "Report Issue" button (Flag icon + label) and user avatar
    - ReportIssueModal with TanStack Form (Title + Description fields), loading state, success state (category + Jira link), error state (preserved form data)
    - Full LLM classification → Jira ticket creation flow via /api/chat-sync
    - Unit tests for buildReportPrompt + parseTicketResponse (all passing)
    - Playwright tests for header visibility and modal flow
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev`
    2. Open http://localhost:3000 — confirm the AppHeader bar is visible above the page content with "Report Issue" button on the right
    3. Visit http://localhost:3000/login — confirm NO header is visible
    4. On a protected page, confirm page content starts below the header (not hidden behind it)
    5. Click "Report Issue" — confirm the modal opens with Title and Description fields
    6. Fill in a real bug report and submit — confirm spinner shows during processing, then success state with category label and Jira ticket URL appears
    7. Click the ticket link — confirm it opens a real Jira issue in a new tab
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase 6, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Unit suite: `pnpm test` — all pass.
Playwright: all 6 browser scenarios confirmed by playwright-qa-tester agent.
Human: full end-to-end flow from button click to Jira ticket creation verified.
</verification>

<success_criteria>
- pnpm test exits 0 (all unit tests including report-issue.test.ts)
- Playwright: header absent on /login, visible on protected pages, modal opens and submits
- Human: observed a real Jira ticket created from the Report Issue form
- Phase 6 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-a-persistent-report-bug-button-on-every-page-that-lets-users-submit-an-issue-the-system-uses-an-llm-to-categorize-the-request-bug-feature-etc-and-then-automatically-creates-a-jira-ticket/06-04-SUMMARY.md`
</output>
