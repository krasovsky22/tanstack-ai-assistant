---
phase: 01-yahoo-mail-ingestion-job-email-tracking
plan: 03
subsystem: api
tags: [tanstack-router, drizzle-orm, postgresql, rest-api]

# Dependency graph
requires:
  - phase: 01-01
    provides: jobEmails table in schema.ts
provides:
  - GET /api/mail/email-count — returns { count: number } for a job's email count
  - GET /api/mail/emails-by-job — returns JobEmail[] sorted by receivedAt desc
affects:
  - 01-04 (UI plan depends on these endpoints for badge count and thread view)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - TanStack Router file-based API routes with server.handlers.GET pattern
    - Drizzle count() aggregation with eq filter for count endpoints
    - Safe fallback: missing query params return empty/zero rather than 404 or 500

key-files:
  created:
    - src/routes/api/mail/email-count.tsx
    - src/routes/api/mail/emails-by-job.tsx
  modified:
    - src/routeTree.gen.ts (auto-generated by build)

key-decisions:
  - "Missing jobId returns { count: 0 } and [] respectively — safe fallbacks, not errors"
  - "emails-by-job selects exactly 6 fields (id, subject, sender, receivedAt, emailLlmSummarized, emailContent) matching Plan 04 UI contract"

patterns-established:
  - "Read-only query routes: import { db } from @/db, filter with eq(), use count() for aggregation, orderBy(desc()) for sorted lists"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-05
---

# Phase 1 Plan 03: Mail Query API Endpoints Summary

**Two read-only Drizzle query endpoints — email-count (badge data) and emails-by-job (thread list) — registered in TanStack Router with safe no-jobId fallbacks**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-05T11:06:29Z
- **Completed:** 2026-03-05T11:07:56Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- GET /api/mail/email-count returns { count: N } using Drizzle count() aggregation, or { count: 0 } when no jobId provided
- GET /api/mail/emails-by-job returns emails array with 6 fields sorted by receivedAt desc, or [] when no jobId provided
- Both routes registered in routeTree.gen.ts and build passes cleanly

## Task Commits

Each task was committed atomically:

1. **Task 1: GET /api/mail/email-count route** - `0f1b0ef` (feat)
2. **Task 2: GET /api/mail/emails-by-job route** - `fc86d80` (feat)

## Files Created/Modified
- `src/routes/api/mail/email-count.tsx` - GET endpoint returning { count: number } for jobId
- `src/routes/api/mail/emails-by-job.tsx` - GET endpoint returning JobEmail[] sorted by receivedAt desc
- `src/routeTree.gen.ts` - Auto-updated by TanStack Router build with both new routes

## Decisions Made
- Missing jobId returns safe fallbacks ({ count: 0 } and []) rather than 400/404 — consistent with plan spec
- emails-by-job projects exactly the 6 fields Plan 04 UI needs; emailContent included for full email body display

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both endpoints ready for Plan 04 (UI) to consume
- email-count provides badge count data per job
- emails-by-job provides the collapsible thread view data sorted newest-first

---
*Phase: 01-yahoo-mail-ingestion-job-email-tracking*
*Completed: 2026-03-05*
