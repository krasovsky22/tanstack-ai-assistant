---
phase: 08-github-mcp-tool-integration
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/routes/_protected/settings.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "GitHubSettingsCard appears on the Settings page below the Jira card"
    - "PAT input shows masked placeholder when a PAT is already saved"
    - "After saving a valid PAT, 'Connected as @username' text is shown"
    - "After saving an invalid PAT, PAT is still saved but no connectedAs shown"
    - "Saving without changing the masked PAT does not overwrite the stored value"
  artifacts:
    - path: "src/routes/_protected/settings.tsx"
      provides: "GitHubSettingsCard component + integration into SettingsPage"
  key_links:
    - from: "src/routes/_protected/settings.tsx"
      to: "/api/user-settings"
      via: "fetch PUT with githubPat in body"
      pattern: "githubPat.*fetch.*PUT"
---

<objective>
Add the GitHubSettingsCard to the Settings page. The card follows the same pattern as JiraIntegrationCard: PAT password input, save button with loading state, masked placeholder when PAT is saved, connection validation feedback showing "Connected as @username" on success.

Purpose: Gives users a UI to store their GitHub PAT, which the backend uses to load GitHub tools into the chat agent.
Output: GitHubSettingsCard rendered below JiraIntegrationCard on the Settings page, with full save/validate/display flow.
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-github-mcp-tool-integration/08-CONTEXT.md
@.planning/phases/08-github-mcp-tool-integration/08-RESEARCH.md
@.planning/phases/08-github-mcp-tool-integration/08-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/routes/_protected/settings.tsx (existing type, must be extended):
```typescript
type UserSettings = {
  jiraBaseUrl: string | null;
  jiraEmail: string | null;
  jiraPat: string | null;
  jiraDefaultProject: string | null;
  hasJiraPat: boolean;
  // ADD these fields (returned by updated /api/user-settings):
  // githubPat: string | null;   // masked '••••••••' when saved
  // hasGithubPat: boolean;
};
```

PUT /api/user-settings response (after Plan 02):
```typescript
{
  jiraBaseUrl, jiraEmail, jiraPat, jiraDefaultProject, hasJiraPat,
  githubPat: string | null,    // masked or null
  hasGithubPat: boolean,
  connectedAs: string | null,  // GitHub username if valid, null if invalid/unavailable
}
```

Existing JiraIntegrationCard pattern to follow:
- Box wrapper: bg="bg.surface" borderRadius="lg" borderWidth="1px" borderColor="border.default" shadow="sm" overflow="hidden" mt="6"
- Header section: px="6" py="4" borderBottomWidth="1px" borderColor="border.default" bg="bg.subtle"
- Header has title Text + subtitle Text + status Badge (Stack direction="row" align="center" justify="space-between")
- Body section: px="6" py="5" with form using Box as="form"
- form.Field for PAT input (type="password", placeholder=MASKED_PAT)
- form.Subscribe for submit button with loading state (colorPalette="brand")

MASKED_PAT constant: '••••••••' (already defined in the file — reuse it)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHubSettingsCard + SettingsPage wiring</name>
  <files>src/routes/_protected/settings.tsx</files>
  <action>
    Update src/routes/_protected/settings.tsx:

    1. Extend the UserSettings type to include:
       ```typescript
       githubPat: string | null;
       hasGithubPat: boolean;
       ```
       Note: connectedAs is only in the PUT response — handle it in local state within GitHubSettingsCard, not in the shared UserSettings type.

    2. Add a GitHubStatusBadge component (mirrors JiraStatusBadge):
       - Props: `{ settings: UserSettings | undefined }`
       - Shows Badge colorPalette="green" "GitHub tools active" when settings?.hasGithubPat is true
       - Shows Badge colorPalette="gray" "Not configured" otherwise

    3. Add a GitHubSettingsCard component (props: `{ settings: UserSettings | undefined }`):
       - Local state: `const [connectedAs, setConnectedAs] = useState<string | null>(null)`
       - Local state: `const [submitError, setSubmitError] = useState<string | null>(null)`
       - useForm defaultValues: `{ githubPat: settings?.githubPat ?? '' }`
       - onSubmit: PUT /api/user-settings with body `{ githubPat: value.githubPat }` only
         - API route merges via upsertUserSettings partial update — no need to resend Jira fields
         - On success: `const data = await res.json(); setConnectedAs(data.connectedAs ?? null)`; show toast "GitHub PAT saved"
         - On res not ok: set submitError from res.json().error or fallback message
         - On catch: set submitError from err.message
       - Card layout:
         ```
         Box (bg.surface, borderRadius=lg, borderWidth=1px, borderColor=border.default, shadow=sm, overflow=hidden, mt=6)
           Box header (px=6, py=4, borderBottomWidth=1px, borderColor=border.default, bg=bg.subtle)
             Stack direction=row align=center justify=space-between
               Box
                 Text fontWeight=semibold "GitHub Integration"
                 Text color=text.secondary fontSize=sm "Connect your GitHub account to enable AI-powered repository tools"
               GitHubStatusBadge settings={settings}
           Box body (px=6, py=5)
             Box as=form onSubmit handler
               Stack gap=5
                 form.Field name="githubPat"
                   FormField label="Personal Access Token"
                     helperText if settings?.hasGithubPat: "A token is currently set. Enter a new value to replace it, or leave as-is."
                     Input type=password placeholder=MASKED_PAT value/onChange/onBlur
                   Text fontSize=xs color=text.secondary mt=1 "Requires: repo, read:user scopes"
                 form.Subscribe
                   Stack gap=3
                     if submitError: Text fontSize=sm color=red.600
                     Box
                       Button type=submit colorPalette=brand loading=isSubmitting loadingText="Saving..." disabled=!canSubmit
                         Save
                     if connectedAs: Text fontSize=sm color=green.600 "Connected as @{connectedAs}"
         ```

    4. In SettingsPage JSX render block (where JiraIntegrationCard is rendered):
       - After `<JiraIntegrationCard settings={settings} />` add `<GitHubSettingsCard settings={settings} />`
       - GitHubSettingsCard goes between JiraIntegrationCard and BrowserNotificationsCard

    5. The useQuery in SettingsPage fetches from /api/user-settings — after Plan 02 the response includes githubPat and hasGithubPat automatically. The UserSettings type extension makes them available. No query change needed.

    UI rules: Chakra UI components only (Box, Stack, Badge, Button, Input, Text, FormField). No raw HTML styling. Desktop layout only (no responsive).
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>pnpm test passes. GitHubSettingsCard exists in settings.tsx. SettingsPage JSX includes GitHubSettingsCard. No TypeScript compilation errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Settings UI + end-to-end GitHub MCP flow</name>
  <files></files>
  <action>No automated action — human verifies the complete feature end-to-end.</action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Human approved: GitHubSettingsCard renders, PAT save flow works, masked placeholder shown on reload, GitHub tools available in chat with valid PAT.</done>
  <what-built>
    GitHubSettingsCard on the Settings page. Service wiring from DB through API to chat agent. Full GitHub MCP integration across Plans 01-03.
  </what-built>
  <how-to-verify>
    1. Start the dev server: pnpm dev
    2. Log in and navigate to http://localhost:3000/settings
    3. Verify: GitHubSettingsCard appears below the Jira card with title "GitHub Integration", subtitle text, and a "Not configured" badge
    4. Verify: The card has a password input field with placeholder "••••••••" and helper text "Requires: repo, read:user scopes"
    5. Enter a real GitHub PAT (or a dummy string) and click Save:
       - With a valid PAT: "Connected as @your-username" should appear below the Save button
       - With an invalid PAT: the PAT is saved but no connectedAs shown (no error blocking save)
    6. Reload the Settings page:
       - Verify: PAT field shows the masked placeholder (not the raw PAT)
       - Verify: badge shows "GitHub tools active"
    7. Open the Chat page and ask "list my GitHub repositories":
       - With a valid PAT: the agent makes GitHub MCP tool calls
       - With no PAT: agent responds without GitHub tools (no error)
  </how-to-verify>
  <resume-signal>Type "approved" if the card renders and save flow works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- pnpm test passes (no regressions)
- GitHubSettingsCard renders on Settings page below Jira card
- PAT save flow shows connectedAs on success
- Masked placeholder shown when PAT already exists
- Badge shows "GitHub tools active" when hasGithubPat is true
</verification>

<success_criteria>
- Settings page shows GitHubSettingsCard with PAT input, scope hint text, and status badge
- Save stores PAT and shows "Connected as @username" on valid PAT
- Masked placeholder preserved on page reload when PAT is already saved
- GitHub tools available in chat agent when user has a valid PAT saved
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/08-github-mcp-tool-integration/08-03-SUMMARY.md`
</output>
