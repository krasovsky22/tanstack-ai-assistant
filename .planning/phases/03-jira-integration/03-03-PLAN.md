---
phase: 03-jira-integration
plan: "03"
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/tools/index.ts
  - src/services/chat.ts
  - .env.example
autonomous: true
requirements: [JIRA-08]

must_haves:
  truths:
    - "getJiraTools is exported from src/tools/index.ts"
    - "buildChatOptions() includes Jira tools under the 'jira' disable key"
    - "JIRA_BASE_URL and JIRA_PAT documented in .env.example with comments"
    - "DISABLE_TOOLS comment updated to list 'jira' as a valid key"
    - "chat.test.ts JIRA-08 tests are GREEN"
  artifacts:
    - path: "src/tools/index.ts"
      provides: "getJiraTools export"
      contains: "getJiraTools"
    - path: "src/services/chat.ts"
      provides: "jira tools wired into buildChatOptions"
      contains: "getJiraTools"
    - path: ".env.example"
      provides: "Jira env var documentation"
      contains: "JIRA_BASE_URL"
  key_links:
    - from: "src/services/chat.ts"
      to: "src/tools/index.ts"
      via: "dynamic import('@/tools') + enabled('jira') guard"
      pattern: "enabled\\('jira'\\)"
    - from: ".env.example"
      to: "src/tools/jiratool.ts"
      via: "JIRA_BASE_URL and JIRA_PAT env vars"
      pattern: "JIRA_BASE_URL|JIRA_PAT"
---

<objective>
Wire the completed jiratool.ts into the project: export from the tools barrel, register in buildChatOptions(), and document environment variables. This is the final integration step that makes Jira tools callable from the chat UI.

Purpose: Connect implementation to the running system — tools are useless without registration.
Output: getJiraTools exported, registered under 'jira' disable key, env vars documented.
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-jira-integration/03-CONTEXT.md
@.planning/phases/03-jira-integration/03-02-SUMMARY.md

<interfaces>
<!-- src/tools/index.ts current state — append one line -->
export { getDockerMcpToolDefinitions } from './docker-mcp';
export { getCronjobTools } from './crontool';
export { getNewsApiTools } from './newsapi';
export { getUiBackendApiTools } from './ui-backend-api';
export { getFileTools } from './filetool';
export { getCmdTools } from './cmdtool';
export { getMemoryTools } from './memory';
export { getKnowledgeBaseTools } from './knowledgebasetool';
// ADD: export { getJiraTools } from './jiratool';

<!-- src/services/chat.ts buildChatOptions() — destructure and register -->
// In the import destructure:
const {
  getDockerMcpToolDefinitions, getCronjobTools, getNewsApiTools,
  getUiBackendApiTools, getFileTools, getCmdTools, getMemoryTools,
  getKnowledgeBaseTools,
  // ADD: getJiraTools
} = await import('@/tools');

// In the tools array (after knowledge_base line):
...(enabled('knowledge_base') ? getKnowledgeBaseTools() : []),
// ADD: ...(enabled('jira') ? getJiraTools() : []),

<!-- .env.example — add Jira section after Elasticsearch section -->
# ── Jira ──────────────────────────────────────────────────────────────────────
# Jira Server REST API v2 integration (optional — tools are enabled when both vars are set)
JIRA_BASE_URL=                # Base URL of your Jira Server instance (e.g. https://jira.yourcompany.com)
JIRA_PAT=                     # Personal Access Token from Jira → Profile → Personal Access Tokens
JIRA_DEFAULT_PROJECT=         # (Optional) Default project key for reference (e.g. PROJ) — not used by tools currently

<!-- DISABLE_TOOLS comment update — add 'jira' to the valid keys comment -->
# Valid keys: mcp, cronjob, news, ui, file, cmd, memory, knowledge_base, jira
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export getJiraTools from src/tools/index.ts and register in buildChatOptions</name>
  <files>src/tools/index.ts, src/services/chat.ts</files>
  <action>
    Step 1 — src/tools/index.ts:
    Append one line at the end of the file:
    ```typescript
    export { getJiraTools } from './jiratool';
    ```

    Step 2 — src/services/chat.ts, function buildChatOptions():

    a) Add getJiraTools to the destructured import:
    ```typescript
    const {
      getDockerMcpToolDefinitions,
      getCronjobTools,
      getNewsApiTools,
      getUiBackendApiTools,
      getFileTools,
      getCmdTools,
      getMemoryTools,
      getKnowledgeBaseTools,
      getJiraTools,         // ADD THIS
    } = await import('@/tools');
    ```

    b) Add to the tools array after the knowledge_base line:
    ```typescript
    ...(enabled('knowledge_base') ? getKnowledgeBaseTools() : []),
    ...(enabled('jira') ? getJiraTools() : []),   // ADD THIS
    ```

    The key 'jira' matches what env var DISABLE_TOOLS='jira' would disable. This is consistent with all other tool registrations in the file.

    Do not change any other part of chat.ts. Do not alter the system prompt, model, or maxIterations.
  </action>
  <verify>
    <automated>pnpm vitest run src/services/chat.test.ts</automated>
  </verify>
  <done>src/services/chat.test.ts JIRA-08 tests are GREEN. getJiraTools is exported from index.ts and called within buildChatOptions when 'jira' is not disabled. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Document Jira env vars in .env.example and update DISABLE_TOOLS comment</name>
  <files>.env.example</files>
  <action>
    Add a Jira section to .env.example. Insert it after the Elasticsearch section (after the last YAHOO_MAIL_MAX_EMAILS line), before the end of the file.

    New section to append:
    ```
    # ── Jira ──────────────────────────────────────────────────────────────────────
    # Jira Server REST API v2 integration (optional — tools activate when both vars are set)
    JIRA_BASE_URL=                # Base URL of your Jira Server instance (e.g. https://jira.yourcompany.com)
                                  # Do NOT include a trailing slash.
    JIRA_PAT=                     # Personal Access Token — generate at Jira → Profile → Personal Access Tokens
    JIRA_DEFAULT_PROJECT=         # (Optional) Default project key for reference, e.g. PROJ — not used by tools in this phase
    ```

    Also update the DISABLE_TOOLS comment line. Find this line:
    ```
    # Valid keys: mcp, cronjob, news, ui, file, cmd, memory
    ```
    And update it to:
    ```
    # Valid keys: mcp, cronjob, news, ui, file, cmd, memory, knowledge_base, jira
    ```

    Per the CONTEXT.md decision: follow existing env var pattern — descriptive comments, mark optional ones.
  </action>
  <verify>
    <automated>grep -n "JIRA_BASE_URL\|JIRA_PAT\|jira" /Users/RandomPotato/Workspace/tanstack-ai-assistant/.env.example</automated>
  </verify>
  <done>.env.example contains JIRA_BASE_URL, JIRA_PAT, JIRA_DEFAULT_PROJECT with descriptive comments. The DISABLE_TOOLS valid keys comment includes 'jira' and 'knowledge_base'.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green check</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions. If any tests fail that were passing before this phase, investigate and fix — do not proceed to summary until all tests pass.

    Run: pnpm test

    Expected: all tests pass including:
    - src/tools/jiratool.test.ts (JIRA-01 through JIRA-07)
    - src/services/chat.test.ts (JIRA-08)
    - Any pre-existing tests from Phases 01 and 02
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>pnpm test exits 0 with all tests passing. No new test failures introduced.</done>
</task>

</tasks>

<verification>
- `grep -n "getJiraTools" src/tools/index.ts` — shows export line
- `grep -n "getJiraTools\|jira" src/services/chat.ts` — shows import and enabled('jira') guard
- `grep -n "JIRA_BASE_URL\|JIRA_PAT\|jira" .env.example` — shows all three vars and updated comment
- `pnpm test` — full suite green
</verification>

<success_criteria>
- getJiraTools exported from src/tools/index.ts
- buildChatOptions() includes ...(enabled('jira') ? getJiraTools() : [])
- .env.example documents JIRA_BASE_URL, JIRA_PAT, JIRA_DEFAULT_PROJECT
- DISABLE_TOOLS comment updated to include 'jira' and 'knowledge_base'
- pnpm test exits 0
- All 8 requirements JIRA-01 through JIRA-08 covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-jira-integration/03-03-SUMMARY.md`
</output>
