---
phase: 05-gateway-identity-linking
plan: "05"
type: execute
wave: 4
depends_on:
  - "05-03"
  - "05-04"
files_modified: []
autonomous: false
requirements:
  - GID-01
  - GID-02
  - GID-03
  - GID-04
  - GID-05
  - GID-06
  - GID-07

must_haves:
  truths:
    - "Settings page renders GatewayIdentitiesCard with Generate Code button"
    - "Clicking Generate Code produces a visible 6-char code in the UI"
    - "Full test suite is green before phase completion"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm the Settings UI card works correctly and the full test suite is green. This is the phase gate before /gsd:verify-work.

Purpose: Visual and interactive verification cannot be automated. The human confirms the UI renders and the linking flow works as designed.
Output: Phase 5 approved or issues reported for gap closure.
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-gateway-identity-linking/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions before handing off to human verification:

    ```bash
    pnpm test
    ```

    All tests must pass. If any test fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>All tests pass. No failures in the full suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gateway identity linking system:
    - DB tables: gateway_identities and linking_codes
    - Service: src/services/gateway-identity.ts with 6 functions
    - API routes: GET/DELETE /api/gateway-identities, POST/PUT /api/gateway-link
    - Gateway handler: /link CODE intercept, unlinked user blocking, userId propagation
    - Settings UI: GatewayIdentitiesCard with Generate Code and identity list
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Open http://localhost:3000 and log in with your account
    3. Navigate to Settings (should be in the nav)
    4. Confirm "Gateway Identities" card appears below "Browser Notifications"
    5. Click "Generate Code" — confirm a 6-char uppercase code appears (e.g. "A3F9C2")
    6. Confirm "Copy" button appears next to the code
    7. Confirm instruction text shows "/link YOURCODE" with the actual code
    8. (Optional live test) Send "/link YOURCODE" to the Telegram bot — confirm bot replies "Account linked!"
    9. Confirm the linked identity appears in the list below (provider=telegram, chatId, date)
    10. Click "Remove" on the linked identity — confirm it disappears from the list
    11. (Optional) Send a message from an unlinked Telegram account — confirm bot replies with linking instructions
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green
- GatewayIdentitiesCard renders correctly in Settings
- Generate Code flow works end-to-end
</verification>

<success_criteria>
- All automated tests pass
- Human confirms Settings UI card renders and Generate Code works
- Phase 5 approved for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/05-gateway-identity-linking/05-05-SUMMARY.md`
</output>
