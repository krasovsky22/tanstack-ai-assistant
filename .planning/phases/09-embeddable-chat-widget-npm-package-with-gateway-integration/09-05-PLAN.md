---
phase: 09-embeddable-chat-widget-npm-package-with-gateway-integration
plan: "05"
type: execute
wave: 4
depends_on: ["09-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Full test suite (pnpm test) passes GREEN"
    - "pnpm build:widget produces dist/chat-widget.js"
    - "Widget opens and sends a message in a browser test page"
    - "LLM reply appears in the widget panel"
    - "CLAUDE.md and .env.example document WIDGET_API_KEY and WIDGET_GATEWAY_URL"
  artifacts:
    - path: "packages/chat-widget/dist/chat-widget.js"
      provides: "Built IIFE bundle"
    - path: ".env.example"
      provides: "Widget env var documentation"
      contains: "WIDGET_API_KEY"
  key_links:
    - from: "packages/chat-widget/dist/chat-widget.js"
      to: "/api/gateway/widget"
      via: "ChatWidget.init() POST + polling GET"
      pattern: "api/gateway/widget"
---

<objective>
Human verification: confirm the full automated test suite is GREEN, the widget build succeeds, and an end-to-end widget flow works in a real browser (floating button opens panel, message sent, LLM reply received). Also update documentation.

Purpose: Phase gate before closing Phase 9 — ensures all automated contracts are met and the widget actually works in a browser context, which no unit test can confirm.
Output: Confirmed working widget + updated CLAUDE.md env var docs
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-embeddable-chat-widget-npm-package-with-gateway-integration/09-CONTEXT.md
@.planning/phases/09-embeddable-chat-widget-npm-package-with-gateway-integration/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated suite green + documentation update</name>
  <files>CLAUDE.md, .env.example</files>
  <action>
    1. Run the full test suite: `pnpm test` — all tests must pass. If any fail, diagnose and fix before proceeding.

    2. Run the widget build: `pnpm build:widget` — must succeed and produce `packages/chat-widget/dist/chat-widget.js`.

    3. Update `CLAUDE.md` — add the three new env vars to the "Environment Variables" section:
    ```
    - `WIDGET_API_KEY` — shared secret for the embeddable chat widget; widget passes this as `x-widget-api-key` header
    - `WIDGET_GATEWAY_URL` — internal URL of the WebWidgetProvider HTTP server (default: `http://localhost:3001`)
    - `WIDGET_INTERNAL_PORT` — port the gateway's WebWidgetProvider HTTP server listens on (default: `3001`)
    ```
    Also add a note to the "Workers" section that the gateway worker now starts `WebWidgetProvider` when `WIDGET_API_KEY` is set.

    4. Verify `.env.example` already has WIDGET_API_KEY, WIDGET_GATEWAY_URL, WIDGET_INTERNAL_PORT documented (they should have been added by Plans 02 and 03). If any are missing, add them now.

    5. Create a minimal test HTML file at `packages/chat-widget/test.html` for manual smoke testing:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head><meta charset="UTF-8"><title>Widget Test</title></head>
    <body>
      <h1>Widget Smoke Test</h1>
      <p>You should see a blue chat button in the bottom-right corner.</p>
      <script src="./dist/chat-widget.js"></script>
      <script>
        ChatWidget.init({
          endpoint: 'http://localhost:3000',
          apiKey: 'your-widget-api-key-here'
        });
      </script>
    </body>
    </html>
    ```
    This file is for developer smoke testing only — not served by the app.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -10 && pnpm build:widget && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>Full test suite passes GREEN. build:widget succeeds. CLAUDE.md documents the three widget env vars. test.html smoke test file exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 9 implementation:
    - WebWidgetProvider in gateway worker (internal HTTP server on port 3001)
    - POST /api/gateway/widget + GET /api/gateway/widget/$jobId proxy routes
    - packages/chat-widget/ with React components, useChat polling hook, IIFE bundle
    - pnpm build:widget produces dist/chat-widget.js
    - ChatWidget.init({ endpoint, apiKey }) mounts floating button on any page
  </what-built>
  <how-to-verify>
    1. Start both servers in two terminals:
       - Terminal 1: `pnpm dev` (TanStack app on port 3000)
       - Terminal 2: `WIDGET_API_KEY=test123 pnpm gateway:dev` (gateway on default port)

    2. Set env vars in `.env` (or shell): `WIDGET_API_KEY=test123` and `WIDGET_GATEWAY_URL=http://localhost:3001`

    3. Build the widget: `pnpm build:widget`

    4. Open `packages/chat-widget/test.html` in a browser. Update the apiKey in the script to `test123`. Open via file:// or a local server.

    5. Verify you see a blue circle button in the bottom-right corner of the page.

    6. Click the button — a chat panel should slide up.

    7. Type a message (e.g. "Hello, are you there?") and click Send.

    8. Watch for the "…" loading indicator while the LLM processes.

    9. Verify an LLM reply appears in the panel within ~30 seconds.

    10. Close and reopen the panel — previous messages should still be visible (memory persistence).

    11. Check browser console — no CORS errors, no "Invalid hook call" errors.

    Expected: All 11 steps work without errors.
  </how-to-verify>
  <resume-signal>Type "approved" if the widget works end-to-end, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` exits 0 (all W9-01 through W9-05 GREEN)
- `pnpm build:widget` exits 0, produces dist/chat-widget.js
- CLAUDE.md includes WIDGET_API_KEY, WIDGET_GATEWAY_URL, WIDGET_INTERNAL_PORT in Environment Variables section
- Human confirmed: floating button visible, panel opens, LLM reply received, memory persists across minimize
</verification>

<success_criteria>
Phase 9 complete: automated tests pass, IIFE bundle builds, widget operates end-to-end in a browser communicating with the gateway worker via the TanStack proxy routes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-embeddable-chat-widget-npm-package-with-gateway-integration/09-05-SUMMARY.md`
</output>
