---
phase: 11-configurable-agents
plan: "05"
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Full test suite is green"
    - "Agents admin page visible at /agents with working CRUD"
    - "Agent API key copyable from admin page"
    - "Agent dropdown appears in Chat UI, pre-selected with default agent"
    - "Dropdown is disabled after first message is sent"
    - "Chat uses the selected agent's model (verified by sending a test message)"
  artifacts:
    - path: "src/routes/_protected/agents.tsx"
      provides: "Agents CRUD page — verified working in browser"
    - path: "src/services/chat.ts"
      provides: "buildChatOptions with agent wiring — verified via test suite"
  key_links:
    - from: "agents admin page"
      to: "/api/agents"
      via: "browser round-trip: create agent, see it in table"
    - from: "chat.tsx agent dropdown"
      to: "selected agent model"
      via: "send message, observe response uses correct model"
---

<objective>
Human verification checkpoint: confirm the full agents system works end-to-end in the browser. This is a blocking gate before the phase is marked complete.

Purpose: Visual and interactive behaviors (dropdown locking, API key masking, Set Default flow) cannot be verified by automated tests alone.
Output: Phase sign-off or issue list for gap closure.
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-configurable-agents/11-CONTEXT.md
@.planning/phases/11-configurable-agents/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
    Run the full test suite and report results. Fix any failures before proceeding to the human verification checkpoint.

    ```bash
    pnpm test
    ```

    If failures exist, diagnose and fix them. Common failure modes:
    - chat.test.ts: missing vi.mock entries for new services/agents import
    - agents.test.ts: DB mock not matching new query pattern
    - TypeScript errors in newly created files

    Only proceed to Task 2 (human checkpoint) when pnpm test exits 0.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    - pnpm test exits 0
    - All test files in src/ pass
    - No TypeScript errors (tsc --noEmit clean)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Configurable Agents system:
    - agents DB table with CRUD API routes
    - /agents admin page: table with name/model/iterations/isDefault badge/masked API key/actions
    - Bot icon in IconRail linking to /agents
    - Agent selector dropdown in Chat UI (above input, locks after first send)
    - Both chat endpoints load agent config from DB
    - Widget API key validation against agents table (WIDGET_API_KEY retired)
  </what-built>
  <how-to-verify>
    Start the dev server: `pnpm dev`

    1. Navigate to /agents
       - Expected: Agents page loads with empty table and "Create your first agent" state
       - Click "New Agent", fill in: Name="Test Agent", Model="gpt-5.2", Max Iterations=10, System Prompt="You are a test agent."
       - Save and confirm the agent appears in the table with all columns populated
       - Verify: isDefault badge is NOT shown (not default yet)
       - Verify: API key column shows first 8 chars + masked suffix + copy button
       - Click the copy button — confirm toast appears and clipboard has the full UUID

    2. Set Default
       - Click "Set Default" on the Test Agent row
       - Verify: "Default" green badge appears on the row
       - Create a second agent "Agent 2", then click "Set Default" on it
       - Verify: badge moves to Agent 2, Test Agent no longer has the badge

    3. Bot icon in navigation
       - Verify: Bot icon appears in the left IconRail
       - Verify: clicking it navigates to /agents and highlights the icon

    4. Agent dropdown in Chat UI
       - Navigate to /conversations, start a new conversation
       - Verify: Agent dropdown appears above the chat input, pre-selected with the default agent
       - Select a different agent from the dropdown (if multiple exist)
       - Verify: dropdown is enabled before sending
       - Send a message
       - Verify: dropdown becomes disabled after the first message is sent

    5. Edit and Delete
       - Click Edit on an agent — verify modal opens pre-filled with current values
       - Update the name and save — verify table updates
       - Delete the non-default agent — verify it disappears from the table
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- pnpm test green
- Browser: agents page CRUD verified
- Browser: agent dropdown in Chat.tsx verified (pre-selected, locks after send)
- Browser: Bot IconRail icon verified
- Browser: Set Default flow verified (badge moves correctly)
</verification>

<success_criteria>
- Full test suite green
- Human verified all 5 browser scenarios above
- Phase 11 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/11-configurable-agents/11-05-SUMMARY.md` following the summary template.

Update STATE.md to reflect Phase 11 complete.
</output>
