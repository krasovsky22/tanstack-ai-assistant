---
phase: 11-configurable-agents
plan: "03"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/routes/_protected/agents.tsx
  - src/components/IconRail.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Navigating to /agents shows a table listing all agents with name, model, maxIterations, isDefault badge, masked API key with copy button, and action buttons"
    - "Users can create a new agent via a modal form (name, model, maxIterations, system prompt textarea)"
    - "Users can edit an existing agent's fields via the same modal"
    - "Users can delete an agent with a confirmation step"
    - "Clicking 'Set Default' on a row marks that agent isDefault=true (clears previous default)"
    - "Bot icon appears in IconRail linking to /agents"
  artifacts:
    - path: "src/routes/_protected/agents.tsx"
      provides: "Agents admin CRUD page"
      min_lines: 150
    - path: "src/components/IconRail.tsx"
      provides: "Bot nav icon for /agents"
      contains: "Bot"
  key_links:
    - from: "src/routes/_protected/agents.tsx"
      to: "/api/agents"
      via: "useQuery + useMutation fetch calls"
      pattern: "fetch.*api/agents"
    - from: "src/components/IconRail.tsx"
      to: "/agents"
      via: "RailIcon to prop"
      pattern: "to.*agents"
---

<objective>
Build the Agents admin page at /agents (full CRUD table + modal form) and add the Bot nav icon to IconRail. Runs in parallel with Plan 02 (wave 2) since it only needs the DB schema types from Plan 01 and uses the API routes that Plan 02 creates.

Purpose: Gives users a way to create and manage agents before the chat wiring (Plan 04) makes them usable in conversations.
Output: agents.tsx page file, updated IconRail.tsx.
</objective>

<execution_context>
@/Users/RandomPotato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RandomPotato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-configurable-agents/11-CONTEXT.md
@.planning/phases/11-configurable-agents/11-RESEARCH.md
@.planning/phases/11-configurable-agents/11-01-SUMMARY.md
@src/routes/_protected/cronjobs/index.tsx
@src/components/IconRail.tsx
@src/components/ui/
</context>

<interfaces>
<!-- API shape that this page will call (created in Plan 02): -->
```typescript
// GET /api/agents → Agent[]
// POST /api/agents → Agent (201)
// PUT /api/agents/:id → Agent (200)
// DELETE /api/agents/:id → 204

interface Agent {
  id: string;
  name: string;
  model: string;
  maxIterations: number;
  systemPrompt: string;
  isDefault: boolean;
  apiKey: string;  // full UUID — page should mask it (show first 8 chars + ****)
  createdAt: string;
  updatedAt: string;
}
```

<!-- RailIcon pattern (from IconRail.tsx): -->
```typescript
<RailIcon
  icon={<Bot size={18} />}
  label="Agents"
  to="/agents"
  isActive={pathname.startsWith('/agents')}
/>
```

<!-- CRUD page pattern (from cronjobs page): useQuery for list, useMutation for create/update/delete,
     Chakra UI Table for the list, Dialog/Modal for create/edit form, Button for actions -->

<!-- Chakra UI copy-to-clipboard pattern (from settings page linking code section):
     navigator.clipboard.writeText(value) + toaster.create({ title: 'Copied!' }) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Agents admin page — table + modal CRUD</name>
  <files>src/routes/_protected/agents.tsx</files>
  <action>
    Create `src/routes/_protected/agents.tsx` following the cronjobs page pattern exactly. Use Chakra UI components throughout (CLAUDE.md mandates Chakra UI for all UI code).

    Page structure:
    - `createFileRoute('/agents')` with a component export
    - `useQuery(['agents'], () => fetch('/api/agents').then(r => r.json()))` for the list
    - State: `editingAgent` (null | Agent), `isModalOpen` (boolean), `deleteConfirmId` (null | string)

    Table columns (using Chakra Table):
    1. Name — text
    2. Model — text (monospace font, e.g. `fontFamily="mono"`)
    3. Max Iterations — number
    4. Default — `<Badge colorPalette="green">Default</Badge>` when isDefault=true, nothing otherwise
    5. API Key — masked display: show first 8 chars of apiKey + "••••••••" + copy button. Copy button calls `navigator.clipboard.writeText(agent.apiKey)` and shows a brief toaster success.
    6. Actions — three `<Button size="xs">` buttons: "Edit", "Set Default" (disabled if already default), "Delete"

    Create/Edit Modal (Chakra Dialog):
    - Form fields: Name (Input), Model (Input, placeholder="e.g. gpt-5.2 or amazon.nova-pro-v1:0"), Max Iterations (Input type="number", defaultValue=10), System Prompt (Textarea rows={6})
    - On create: POST /api/agents with form values; on success invalidate agents query
    - On edit: PUT /api/agents/:id; on success invalidate agents query
    - "Set Default" row action: PUT /api/agents/:id with `{ isDefault: true }` (only isDefault field update); on success invalidate agents query

    Delete flow:
    - Clicking Delete sets `deleteConfirmId = agent.id`
    - Show a simple inline confirmation (second click confirms) OR a small AlertDialog — use whichever Chakra pattern is simpler. On confirm: DELETE /api/agents/:id, invalidate agents query.

    Empty state: when no agents exist, show a descriptive message with a "Create your first agent" button.

    Page header: "Agents" heading + "New Agent" button (top right), following cronjobs page layout.

    IMPORTANT — use the exact mutation/invalidation pattern from cronjobs: `queryClient.invalidateQueries({ queryKey: ['agents'] })` after every mutation. All fetch calls in mutations should be typed correctly. Do NOT use TanStack Form for this page — simple controlled inputs via useState are sufficient (cronjobs page uses this simpler pattern and it fits the modal scope).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep "agents.tsx" | head -10</automated>
  </verify>
  <done>
    - src/routes/_protected/agents.tsx exists with no TypeScript errors
    - Page renders the table with correct columns
    - Create/edit modal has all 4 form fields
    - API key column shows masked value with copy button
    - All CRUD mutations call the correct API endpoints
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Bot nav icon to IconRail</name>
  <files>src/components/IconRail.tsx</files>
  <action>
    Update `src/components/IconRail.tsx`:

    1. Add `Bot` to the lucide-react import: `import { Bell, Bot, Briefcase, BookOpen, Clock, MessageSquare, Mail, Settings } from 'lucide-react';`

    2. Add `isAgentsActive` variable after the existing active-state variables:
    ```typescript
    const isAgentsActive = pathname.startsWith('/agents');
    ```

    3. Add the Agents rail icon inside the `<Flex direction="column" gap="1" flex="1">` block, positioned after the Automation (cronjobs) icon and before Notifications:
    ```typescript
    <RailIcon
      icon={<Bot size={18} />}
      label="Agents"
      to="/agents"
      isActive={isAgentsActive}
    />
    ```

    The Bot icon is from lucide-react and is already available in the project (same lucide-react import used for all other icons). No new dependency needed.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep "IconRail.tsx" | head -5</automated>
  </verify>
  <done>
    - IconRail.tsx imports Bot from lucide-react
    - Bot RailIcon renders with to="/agents" and isActive when pathname starts with /agents
    - pnpm tsc --noEmit clean on IconRail.tsx
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` — no errors in agents.tsx or IconRail.tsx
- `pnpm test` — full suite still green (no regressions from UI-only changes)
- Route /agents is registered in TanStack Router (routeTree.gen.ts will auto-update on next dev server start)
</verification>

<success_criteria>
- agents.tsx page: table with 6 columns, create/edit modal, delete confirmation, Set Default action
- API key column: first 8 chars visible + masked suffix + copy-to-clipboard button
- IconRail: Bot icon links to /agents, highlights when active
</success_criteria>

<output>
After completion, create `.planning/phases/11-configurable-agents/11-03-SUMMARY.md` following the summary template.
</output>
